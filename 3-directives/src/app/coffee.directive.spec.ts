import { CoffeeDirective } from './coffee.directive';

describe('CoffeeDirective', () => {
  it('should create an instance', () => {
    const directive = new CoffeeDirective();
    expect(directive).toBeTruthy();
  });
});
